# Implementation validation — in progress

Change: `fix-benchmark-preparations-spec`. Preparation is not ready. Historical partial qualification attempts were stopped; the Sep24 32-cell pilot failed at cell 17 and its native reset-check defect is repaired, pending physical renewal. No Q4 or full-matrix arms have been launched by this change. Draft PR #2 continues the approved planning change after the explicitly authorized recovery from merged PR #1.

## Verified repairs

- The native client validates bounds before allocation, hashes its owned snapshot, rejects a wrong supplied digest before exchange, and seals the same bytes. The discovered Linux regression compiles with existing GLib tooling. Its cases cover the 64-MiB boundary, synchronized caller-buffer reuse and descriptor cleanup. The wrong-digest case failed against the original implementation and passed after repair.
- Guardian control validation now precedes request reservation and payload verification. The focused tests cover attributed and unattributed rejections, first-failure concurrency, bounded immutable diagnostics, unsafe descriptors and persistence failure. A pre-authority failure cannot commit an orphan diagnostic.
- All four service/supervisor API/CLI unexpected-retry defaults are zero. Omission tests verify the default and its forwarding; supervisor behavior tests verify one launch, permanent exit 78 and no retry sleep. Existing explicit retry budgets and transient exit 75 recovery remain covered.

## Focused Linux test results

Executed on 2026-09-21 using the existing frozen CPython 3.12 environment, without installing dependencies:

- Native client runner: 1 test passed, including embedded C++ cases.
- GStreamer analytics bridge: 8 passed.
- Production sidecar: 50 passed.
- Analytics protocol: 4 passed.
- Full-publication supervisor: 13 passed.
- WSL service materialization: 25 passed.
- Storage recovery: 10 passed.
- Qualification execution closure: 13 passed.
- Failed-guardian evidence snapshot: 9 passed, including a diagnostic emitted by the real sidecar failure path. Existing lifecycle v1 fixtures and the successful-only qualification closure remain unchanged. A read-only check against actual A269 authority/lifecycle files returned explicit historical diagnostic absence, created no snapshot and preserved both source hashes. Second/third-write failures also preserve sources and partial output, report no success, reject reuse of that partial path and permit a fresh snapshot path. The collector does not claim atomic directory publication.

The combined verification captured unchanged before/after source hashes. Initial harness invocations were retained as failures: `-S` hid the environment's required NumPy/YAML packages, and `-I` prevented a storage-test import of the repository's `tests` namespace. Corrected invocations passed; no source or dependency change was needed to resolve those harness errors.

Reproduce each module from the repository root with the verified frozen interpreter:

```bash
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_checkpoint_analytics_execution_client_cpp.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_checkpoint_gstreamer_analytics_bridge.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_checkpoint_gstreamer_analytics_sidecar.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_analytics_execution_protocol.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_full_publication_supervisor.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_full_publication_wsl_user_service_v1.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_publication_storage_recovery.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_publication_policy_qualification_execution_closure_v1.py -v
"$PYTHON" -B -X utf8 -m unittest discover -s tests -p test_benchmark_preparation_evidence_v1.py -v
```

## Image and operational gates

The three native foundations passed deterministic A/B rebuilds with identical image IDs within each pair, stock source/context checks and smoke execution. Their identities changed as expected. The two dependent analytics worker images also rebuilt successfully against the new native receipt. All four runtime images then rebuilt and passed stock capture/patch verification. Packaged tests found that OpenVINO GVA and GStreamer Custom omitted a module required by the bridge/guardian. Both Dockerfiles now copy that module into the final runtime and exercise real guardian imports during the build. Those two images were rebuilt again and replacement receipts/patch verified; their earlier receipts are retained as superseded evidence.

Current packaged protocol/bridge/sidecar suites pass in all four runtimes; both worker protocol suites pass. Existing packaged policy, clock/backend and external-manifest checks also pass on the current images. Fifteen image/source-closure tests passed. An affected CUDA-transfer test initially failed to compile because its command lacked GLib flags; its corrected compiler invocation passed both tests. No extra runtime dependency was installed.

The fresh physical parity materializer completed its original invocation with exit 0: 480 executions in 32 groups, 15 per group. The stock acceptance loader returned successfully and the physical descriptor traversal passed. A later independent-comparison wrapper then failed because it looked for the historical reference in the isolated checkout; that failure was retained. A separate comparison against the original read-only reference passed, with all 480 input/output tensor hashes matching.

Host identity fixtures were then renewed in the four host-only files so OpenVINO GVA and GStreamer Custom constants match the rebuilt `:materialized` images (`sha256:261ef8a6…` and `sha256:4e1cbec0…`). Thirteen focused identity tests passed in 0.194s. The stock v4 acceptance loader then reloaded the current receipts (`acceptance_identity_sha256=7d6fd530…`). Live preflight was not executed: the accepted assessment identity is bound to the worktree `models/` inventory without OMZ proxy files, and `_omz_bindings` requires those files at `models/openvino`. The live preflight test now skips unless both Docker and those OMZ files are present.

The environment baseline records the frozen interpreter/packages, OS/kernel/WSL/Docker, CPU/GPU/driver and power/resource settings, current data/model/image/parity descriptors, 24-GB WSL configuration and process ownership. A269 remains retired; no workers or containers remained after parity. These observations do not replace final live preflight.

The first final ext4 suite ran 2506 tests in 1978.988s and ended FAILED (failures=4, errors=20, skipped=88). After-manifest comparison then stopped on `fixtures.source` because `launch.stdout.log` was written into the snapshotted orchestration directory. The failed tree is preserved as `artifacts/full_suite_ext4_fix_benchmark_preparations_20260921_final.failed-20260921-1732`. Follow-up repairs: LF gitattributes for json/yaml, OMZ files restored under `models/openvino`, recursive data/model mounts, orchestration directory removed from fixtures, and `build_native_probe_images.sh` now delegates to the publication v1 wrapper. Live preflight stays skipped unless `VAST_LIVE_PREFLIGHT=1`. Later suites bound fragment source pins to freeze receipts, restored A59 worker IDs, accepted ext4 peercred custody, and rebased Docker 29 inspect sources onto the current project root.

The first accepted final ext4 suite ran 2513 tests in 2026.894s with exit 0, `OK (skipped=88)`. Qualification then required OMZ proxy files at `models/openvino/public` for fragment `_omz_bindings`. Those files change `local_model_inventory`, so the v4 acceptance was re-promoted (`acceptance_identity_sha256=15bebf45…`) and a renewed final ext4 suite was run on those bytes.

The renewed suite ran 2513 tests in 2243.583s with exit 0, `OK (skipped=88)`, and unchanged source/mirror/fixture/recovered manifests. Independent verification hashed 2219 current files. Source/mirror identity is now `82f080f7…`. The skip-identity review against A136's 87 skips still finds one added skip: live preflight behind `VAST_LIVE_PREFLIGHT=1`. No historical skips disappeared. `publication_ready` remains false. The pre-OMZ suite tree is preserved as `artifacts/full_suite_ext4_fix_benchmark_preparations_20260921_final.accepted-5.4-before-omz-reaccept`.

The renewed suite and image receipts above were accepted before a later native runtime defect was discovered. A fresh qualification guardian and pilot reached 12 cells before a transient Savant NVML sampling gap; the supported same-input resume reached 16 cells, then the first OpenVINO cell failed. Its packaged native probe rejected `openvino_gva`, and a subsequent diagnostic rejected the actual hyphenated worker ID. The original guardian was stopped through its authenticated control path with a clean, nonpublication lifecycle; the partial pilot checkpoint is retained as failed historical evidence. These two native-probe predicates have been repaired within the approved four-system scope. Because the native source changed, the earlier image receipts, 480-execution parity, 2513/88 ext4 suite, qualification preparation and 16-cell checkpoint cannot authorize a new attempt. New deterministic native and dependent-image receipts, parity, final suite and a distinct 32-cell qualification are required. The previous 2513/88 result remains a historical successful execution, not the current final-source gate.

On 2026-09-23, the source-closure invalidation covered all three native probe foundations, two dependent analytics workers, four runtime images, host identity pins, physical parity, final ext4 source/fixture manifests and qualification input receipts. Independent A/B native rebuilds produced byte-identical receipts for all three images. The two workers and four runtimes rebuilt from that receipt; stock capture and patch verification passed. Twelve packaged historical/native checks and ten packaged protocol/bridge/sidecar/worker checks passed on the new images; the direct native C++ topology regression and the focused frozen-identity suite also passed. A single fresh 480-execution parity run completed with exit 0. Its stock acceptance loader and independent descriptor audit passed: 32 groups with 15 samples each, 3,535 descriptor references, 2,478 unique verified files, and all 480 input/output tensor hashes matching the read-only A244 reference. Acceptance identity is `f089b711…`. These results renew tasks 5.1–5.3. The renewed final ext4 suite passed on this source; qualification/Q4/capacity/preflight gates remain pending.
The 2026-09-23 final ext4 suite ran 2,515 tests in 2,331.133 seconds and exited 0 (`OK (skipped=88)`) under its original systemd owner. Its source, mirror, fixture and recovered-fixture before/after manifests matched exactly; an independent verifier rehashed 2,272 current files. Compared with the historical 87 skips, the sole added skip is the deliberate `VAST_LIVE_PREFLIGHT=1` gate, and no historical skip disappeared. The post-suite environment inventory recorded no active containers or duplicate workers; the old failed qualification unit remains historical. The two earlier unsuccessful 2026-09-23 suite attempts remain retained as failed evidence and do not authorize qualification. This result completes the current source-closure test gate only; fresh qualification, Q4, capacity and live preflight are still required.

The operator stated 600 GiB of currently guaranteed free capacity for the existing Seafile destination on 2026-09-23. This is a dated capacity statement, not yet a capacity attestation: Q4's 280 sizing pairs, formula check and live upload/readback are outstanding. Fresh accepted qualification, Q4, live preflight and the unstarted service package remain pending.

The new preparation runbook passed syntax-only Bash parsing, its explicit variable allowlist and parser-only CLI checks. README, PLAN and progress link it while retaining historical A269 status and numbered PLAN obligations.

On 2026-09-23 the fresh qualification input chain completed under its original one-shot unit with exit 0. The transaction remained nonaccepted and bound 32 cells; preprocessing passed its stock validator. A single new guardian (InvocationID `4b4f6bf26cd7408fb5428eb85e8acc77`) issued a live nonpublication authority for eight workers. WSL's older hidden `/mnt/e` bind entries confused the stock mount helper, so the empty visible socket/evidence/runtime targets were bound to the exact ext4 sources and checked by device/inode, filesystem type and runtime read-only status before guardian launch; no project source was changed for that operational workaround. Runtime-input materialization (InvocationID `1444da6bb1a34b70a7ee6e6ef4a5f923`) exited 0 and produced 32 bundles and four generated assets. Independent physical hashes matched all 32 bundles, four assets and captured Docker client. The 180-second Savant diagnostic (InvocationID `3876fa9260d34efaa3e913c07a0808af`) exited 0, retained matching container-log hashes, and recorded `diagnostic_runtime_returned_no_promotion`, `accepted=false`, `publication_ready=false`. One fresh 32-cell pilot executor was then launched (InvocationID `920b5cfea7474bcdbebb44434459bd3d`); its checkpoint appeared in `in_progress` state with zero completed cells, and its original terminal remains pending. No Q4 or full-run arms have started.

## 2026-09-24 qualification failure and renewed closure

The one-shot 2026-09-23 pilot finished with exit 1 after 16/32 cells (eight DeepStream, eight Savant). Its checkpoint remains `in_progress` only as failed historical evidence; it is not a resumable accepted qualification. The first OpenVINO GVA CPU/H264/independent-processes cell failed before the common start barrier. A separate nonpromoting, read-only-input diagnostic retained native stderr: all 24 processes exited 2 because the frozen GStreamer registry exposed no preregistered H264 hardware decoder. On the same pinned OpenVINO image under GPU injection, `nvh264dec` is installed and discoverable when the registry is refreshed, but absent from the frozen build-time registry with `GST_REGISTRY_UPDATE=no`.

`scripts/checkpoint_gstreamer_runtime.py` had passed `refresh_hardware_plugins=not publication_mode` even though its seeding helper already performs GPU-aware decoder discovery, hashes the seed, copies it per process and fails closed if no required decoder exists. Publication now enables that existing refresh. `tests/test_checkpoint_runtime.py` adds a publication-path regression; its 43-test suite and the 11-test codec/refresh suite passed under the frozen WSL interpreter. The pilot's guardian was stopped through its authenticated authority: lifecycle `clean_stop_nonpublication`, eight worker sockets retired, no failed guardian requests, and `publication_ready=false`. Failed pilot and diagnostic evidence remain private and unchanged.

This source is packaged in all four publication runtimes. Native-probe and analytics-worker image source closures are unchanged, subject to exact-byte/image verification; the four runtime images/receipts, identity patch, parity v4 accepted identity, final ext4 suite and Sep23 qualification chain are stale for a new attempt. The parity manifest binds the exact patch descriptor and its execution config derives from the patch hash, so the prior 480/32 acceptance cannot be relabeled. Tasks 5.2-5.4 and 6.1-6.3 were reopened pending physical renewal. No Q4 or full-run arm has started.
The Sep24 four-runtime stock builder completed its deterministic A/B builds with exit 0. Four fresh physical freeze receipts and image patch `8237c580b330de415b0d5f6f947163406e9b644283b588c705d8ac7fc23b5785` were captured. The three native-probe and two analytics-worker image IDs, their receipt file hashes and source sets remain identical to the Sep23 accepted producer receipts; all four runtime image IDs changed. The patch correctly reports `candidate_binding_eligible=false` pending parity refresh. The rebuilt OpenVINO image `sha256:201628a948ed83965ac825444a6a513c7c740300cc2361bcf8afedf7831e2e05` passed two network-isolated GPU-aware registry smokes: packaged runtime source SHA `cd6800c43f9dff2214b5e54c7f7d8f7723f19fe9d751dc4c658a7e8ab6b3152a` seeded `nvh264dec`, both per-process copies had the same SHA `41b4045453fd185bb985a99593c11d01f72974bc32bb61d9d0a4f4eab297adae`, and `gst-inspect-1.0` found that decoder with `GST_REGISTRY_UPDATE=no`. These smokes were diagnostic only (`accepted=false`).

The renewed packaged historical sweep passed 12 image checks and the host CUDA-transfer check. The ten packaged analytics/bridge/sidecar checks passed under original systemd InvocationID `f4e7a4eca6064d67b91fb38dd5333c89` with exit 0; stock source-closure (15), build-context (8), refreeze (11), frozen-constant (9, one live skip), OpenVINO host (14, one live skip) and GStreamer host (6) suites also passed. The initial refreeze test invocation without `PYTHONPATH=scripts` failed on module import; its corrected frozen-interpreter invocation passed all 11 tests without changing source. Task 5.2 is renewed. Physical parity, the final ext4 suite and fresh qualification remain pending; no later gate is admitted by these checks.
The 2026-09-24 patch-bound parity materializer completed its original systemd invocation babeeb43995649b0bd89243f713a38c0 with exit 0. Its new accepted identity is 73b22dbbbdbc71dcb61ab56ed14a93fdbcf60cbe784d7bd5dcf91e9b9ce8b6e5. A separate frozen-interpreter audit invoked the stock acceptance loader and verified 3,535 descriptor references, 2,478 unique files, all 480 physical executions in 32 groups of 15, and every input/output tensor hash against the read-only A244 reference; the audit exited 0 with empty stderr. The original success and independent audit are retained privately under artifacts/fix_benchmark_preparations_20260924/. The GStreamer host fragment pins and focused identity tests were renewed from this accepted receipt; the frozen-interpreter GStreamer/frozen-identity and dependent OpenVINO tests passed. DeepStream and Savant retain their separate baseline parity identities. The complete final-source ext4 suite has passed and is independently verified before qualification.

The Sep24 ext4 suite first created an exact-byte mirror and six before-manifests, then stopped before test discovery because two worktree corpus bind views were empty. The two exact worktree corpus paths were rebound to their existing source directories and verified by inode and receipt bytes. A guarded continuation stopped before test discovery because the systemd root environment omitted the publication runtime path; it retained the two read-only corpus mounts. Both failed InvocationIDs and plans are preserved privately. A third, separately recorded owner began the full test run only after rechecking source/mirror/fixture/recovered hashes, all mounts, corpus receipts and the frozen runtime path. No bootstrap failure is counted as a passed suite.

The third Sep24 ext4 owner, InvocationID fa24a02b269e46b99f04ae6398917898, reached its original successful terminal with exit 0. Complete unittest discovery passed 2,516 tests with zero failures and 88 skips. The independent checker verified the sole added skip against the historical A136 list: the live preflight test explicitly requires VAST_LIVE_PREFLIGHT=1, Docker and OMZ proxy models; no historical skip disappeared. It rehashed 2,286 current source/fixture/recovered files and proved all six source/mirror/fixture/recovered before/after manifest groups unchanged and exact-byte matched. Accepted suite evidence is retained at artifacts/full_suite_ext4_fix_benchmark_preparations_20260924_final/ and artifacts/fix_benchmark_preparations_20260924/ext4_orchestration/final-ext4-suite-check.json. Task 5.4 is complete; fresh 32-cell qualification, Q4, capacity and preflight are still open.

## 2026-09-24 fresh qualification in progress

The fresh input transaction completed under original InvocationID `7d836f744c974174a5537820086e0455` with exit 0. Its receipt SHA-256 is `95d4ba5313934249055eafec718c1057b4b5ec4886123fec16340faed37709f5`; an independent physical audit verified 18 distinct descriptors and all 32 cell bindings. Preprocessing then completed under InvocationID `18a405301fba4a408547f5de11669c92` with exit 0; the frozen contract and receipt hashes are `aba5e9ccd3ebfcc87e7bc1e71e4d693cee8bfc7ca3670cf09a9b78963241c052` and `ebca08058b0ec60c977577a77a053101bbf78a4ac63b460ac1eb29a207a6bc77`. The new guardian, InvocationID `d784dc0a11574cd385cc0c20d27e0cc0`, authenticated eight workers and remains live in nonpublication mode.

WSL placed another 9p `/mnt/e` mount above the ext4 socket/evidence/runtime bindings, hiding their visible paths. The first runtime-input service, InvocationID `314f182d72904ca3b7b466245862789f`, failed its mount preflight with exit 1 before creating bundles; that failure is retained. A guarded repair rebound the three exact ext4 sources onto the current visible mount, preserved the runtime read-only flag, verified source/target inodes and ran all three stock check-only helpers. Retry InvocationID `6d13dd938a5c4a6a8f898ca037eb2a09` finished with exit 0. Its materialization receipt SHA-256 is `157170d231b12acad0b3b0be1ba194f38061a3be0b8fd8e5022f0b865bdd4fe6`. Independent audit verified canonical self-hashes, 32 distinct physical bundles covering 4×2×2×2, four generated assets and the copied engine asset. The materializer's own cold validation ran; these inputs remain nonaccepted and nonpublication. The cause of the WSL overmount is not yet proven, so mount visibility remains an operational risk to observe during the pilot.

The bounded Savant CPU/H264/independent-processes diagnostic passed its read-only identity preflight and original service InvocationID `ad3380e4088b4492b8ac42b98429917f` exited 0. Its private diagnostic state SHA-256 is `7fd594c9b2899a5e586847e8a8388b1cb02c66476a939fd787d298706b4ddbf6`: exactly one 180-second container exited 0, six streams completed 468 frames, retained log hashes match, and `accepted=false` / `publication_ready=false`. The first check-only invocation exposed an overly narrow 16-MiB reader bound for the frozen 18.2-MiB numpy wheel; the private helper now uses a 32-MiB bound for allowlisted sources and kept held-descriptor identity checks. No diagnostic evidence was promoted into qualification.

The one fresh 32-cell pilot launched under InvocationID `d6a86cf4cee04a6a9bce3411f4f4a546` after the diagnostic and mount checks. At 2026-09-24 20:08 UTC, its single new checkpoint is `in_progress` with zero committed cells; original terminal and acceptance remain pending. No historical A269/Sep23 cells are counted. Q4, capacity attestation, live preflight and the unstarted full-run package remain outstanding.
## 2026-09-25 qualification failure and native reset-check repair

The Sep24 pilot (InvocationID `d6a86cf4cee04a6a9bce3411f4f4a546`) exited 1 at 2026-09-25 00:51:56 UTC on cell 17 of 32, the first OpenVINO GVA CPU/H.264 independent-processes cell. An independent prefix audit verified its 16 completed cells and 256 physical evidence files; they remain historical and cannot count toward or combine with a new attempt. Three isolated nonpromoting replays of the exact failed-cell bundle captured native SIGSEGV exits before the common start barrier. The symbolized stack ended in `g_value_reset` inside `NativeProbeRuntime::verify_checkpoint_reset_state_before_ready`; a property-type replay observed GstAppSrc `current-level-buffers` as `guint64` while queue elements expose `guint`. The reset check read every such property into a `guint`, so the 64-bit write corrupted the adjacent iterator `GValue`. The guardian (InvocationID `d784dc0a11574cd385cc0c20d27e0cc0`) was stopped through its authenticated control path: `clean_stop_nonpublication`, zero failed requests/connections, `publication_ready=false`.

The repair adds `read_current_level_buffers`, which reads through a `GValue` of the declared type (`G_TYPE_UINT` or `G_TYPE_UINT64`) and throws on unreadable or other types, and extracts the reset-path loop as `verify_pipeline_queues_empty` with cleanup on every exit. The new `tests/cpp/checkpoint_native_reset_queue_level_test.cpp` runs in the discovered native runner. It uses a real `appsrc ! queue ! fakesink` pipeline (asserting the two widths), a 2^32 guint64 level that a 32-bit read would truncate to zero, mixed-width empty and nonempty queues, and unsupported/unreadable property types, with GLib warnings fatal. Against a scratch copy restoring the old `guint` read, it aborted with `*** stack smashing detected ***` (exit 134). With the repair, the 3-test native runner passed under WSL (GStreamer 1.24.2, frozen interpreter), as did the repository-integrity (3), checkpoint-runtime (43), scenario-planning (74), full-resource v2 (11), CUDA-transfer intervals (2), OpenVINO/GStreamer image source (4/6), publication-matrix (8), full-publication entrypoint (62), benchmark-contract (112, one skip) and resource-interval (19) suites. The scenario-planning suite first failed on the stale `fanout_binding_sha256` pin for the probe source; the pin in `configs/experiments.yaml` was renewed to `d1d1755c…`, following the Sep23 precedent.

The operator chose to treat this repair as within the approved preparation scope rather than re-reviewing the planning commit; the decision is recorded in implementation-evidence.md. The probe source is in three native-probe allowlists, so the nine-image chain, patch, parity, final ext4 suite and qualification chain are invalidated again (image-invalidation.md). Tasks 5.2-5.4 and 6.1-6.3 are reopened pending physical renewal. No Q4 or full-run arm has started.

### 2026-09-25 renewal progress

Independent native-probe A/B builds (InvocationIDs `77c2e83448ee477ab15c0570c1595800`, `03fa285c5abb497ab4a74a495f670058`) exited 0 with byte-identical receipts `0f5647e5…` and new image IDs for all three foundations. The two analytics workers (`a12ade38…`, `4c4cbe52…`) and four runtimes (DeepStream `a1ce4b28…`, Savant `f0ff2a49…`, OpenVINO GVA `c5aadcf6…`, GStreamer Custom `4d035545…`) rebuilt from that receipt; stock capture, assemble and verify-patch exited 0 (patch `e98b49941ccdf1b6b516443809c543f938b3be58cce90a6472656dbd75ea79ee`, `candidate_binding_eligible=false` pending worker parity). OpenVINO GVA and GStreamer Custom host pins in four scripts and two tests were rebound by an exact Sep24-to-Sep25 patch value mapping (37 values, no conflicts); the Sep24 parity configs remain historical. The packaged sweep passed 12 historical/native image checks plus the host CUDA-transfer check, and all 10 packaged protocol/bridge/sidecar/worker checks. Task 5.2 is renewed.

The patch-bound parity materializer (InvocationID `3e6dd523f072400ab8a2d36c87667e26`) exited 0 with empty stderr after 480 executions. A separate frozen-interpreter audit (InvocationID `ebccbfb96d394f76ac9f5e136ac1e4cc`) called the stock acceptance loader once and verified 3,535 descriptor references, 2,478 unique files, 32 groups of 15 and all 480 tensor hashes against read-only A244; acceptance identity `4a37e0128bafe385bb475fdf710afcb669f9341e5ad568966a0c1f7c57f6a06c`. The GStreamer Custom fragment and two tests were rebound to that receipt (paths and hashes only; sizes unchanged). The frozen-identity (9, one live skip), four runtime-constant, fragment (11), source-closure (15+2), build-context (8), refreeze (11) and OpenVINO host/launcher suites pass. Task 5.3 is renewed. The final ext4 suite, fresh qualification chain, Q4, capacity and preflight remain pending.

The Sep25 final ext4 suite ran under root systemd InvocationID `18653d7e13794e058dfb745ada569e1b` after the two worktree corpus views were rebound read-only to their exact sources (inode and receipt bytes verified). Complete unittest discovery ran 2,517 tests in 2,105.5 seconds and exited 0 (`OK (skipped=88)`). The independent checker verified the sole added skip is the reviewed `VAST_LIVE_PREFLIGHT=1` gate with no historical A136 skip removed, rehashed 2,339 current source/fixture/recovered files, and proved all six before/after manifest groups unchanged and exact-byte matched (source/mirror `58ccee83…`, fixtures `644c21db…`). Task 5.4 is renewed; the fresh qualification chain, Q4, capacity and preflight remain pending.

### 2026-09-25 fresh qualification chain

Host clearance at 2026-09-25T13:06Z met the WSL-growth commit headroom and 20-GiB reserve on C:, E: and WSL ext4. The input transaction (InvocationID `e90bb45a51614475b09793561ea97c3a`) re-verified the accepted suite, checker, patch and parity pins and exited 0 with empty stderr; receipt `5f262afd0638275b1bb5ff4e330e457bda07740b4209358b49b722abdfad0203`, 18 physical descriptors and 32 cells independently verified. Preprocessing (InvocationID `609e7f9a6b444a169140289bfecbd632`) exited 0; the frozen contract is unchanged (`aba5e9cc…`, identity `0307abfe…`) with new receipt `a2961686…`. WSL again hid the three canonical ext4 binds beneath a newer `/mnt/e` mount; the guarded Sep24 repair rebound them and all three stock check-only helpers passed. A new guardian (InvocationID `52efb1b6dbaf4578a1735b02d83401be`, lifecycle `77250c37…`) attested eight workers on the renewed worker images and remains live in nonpublication mode.

The first runtime-input service (InvocationID `f579572b5fde4b6da361aa7d63fbdc69`) failed its mount recheck in 66 ms before creating bundles because `/mnt/e` was replaced again (mount 1530 to 1628); that failure is retained. The guarded repair then ran in the same root session as retry1 (InvocationID `1798b46200fc44329ae127d21a8e11b9`), which exited 0: 32 bundles, receipt `3875a15c…`, independently audited with 32 unique arms/run IDs/paths/hashes and four generated assets. Task 6.1 is renewed.

Two Savant diagnostic check-only units failed before any container call (a transient launcher path error, then a wrong `--expected-runtime-source-sha256` value that the diagnostic correctly rejected; the second's stderr log was overwritten by the corrected rerun). The corrected check (InvocationID `439436d57f9b4b4f8c5a8f9e678b4455`) passed, and the gated 180-second diagnostic (InvocationID `68179f53306b409dbd93c81975ad6adf`) exited 0 on image `f0ff2a49…`: one container, return code 0, retained log hashes matching, `diagnostic_runtime_returned_no_promotion`, `accepted=false`, diagnostic SHA-256 `97be5a1b…`. Task 6.2 is renewed.

One fresh 32-cell pilot launched under InvocationID `0461b7a92d074b42917fecec8955bd6a` after a repeated guarded mount repair and recheck, with exactly the eight guardian workers running. Its new checkpoint is `in_progress` with zero completed cells (matrix `48b7f9d5…`). No A269, Sep23 or Sep24 cells are reused. Task 6.3 is renewed; 6.4, Q4, capacity and preflight remain pending.

## Evidence and review limits

Detailed operational inventories, physical paths and raw logs remain local. This document summarizes actual checks; it is not a runtime authorization receipt or a CI result. A full service launch was not performed by the focused tests. Required final scenario conformance and latest-commit checks remain outstanding. CI setup is outside this change's approved scope; no CI success or merge readiness is claimed.
